import PRODUCTS from './products'
import AUTH from './auth'
import NAVBAR from "./navigation_bars"

export default class CONFIG {
    static PRODUCT = PRODUCTS
    static AUTH = AUTH
    static NAVBAR = NAVBAR
}